# Flask & Core Dependencies
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0
Werkzeug==3.0.1

# API & LLM
groq==0.5.0
langchain==0.1.20
langchain-community==0.0.38

# Vector Database & Embeddings (Memory Optimized)
chromadb==0.4.24
sentence-transformers==2.7.0

# PyTorch CPU-Only (Critical: Must use PyTorch index to avoid CUDA)
torch==2.1.0 --index-url https://download.pytorch.org/whl/cpu

# PDF Processing
pypdf==4.0.1

# Production Server (REQUIRED for Render)
gunicorn==21.2.0

# Utilities
requests==2.31.0
numpy==1.24.3

# Security & Environment
python-dateutil==2.8.2
